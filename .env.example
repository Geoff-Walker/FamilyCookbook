# WalkerFCB — local development environment variables
# Copy this file to .env and fill in real values.
# .env is gitignored and must never be committed.

# PostgreSQL password for the 'walker' database user
POSTGRES_PASSWORD=changeme

# OpenAI API key used by the backend for recipe embedding (text-embedding-3-small)
OPENAI_API_KEY=sk-...
